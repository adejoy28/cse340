    <!-- Display Title -->
    <h1><%= title %></h1>

    <% if (errors) { %>
    <ul class="notice">
        <% errors.array().forEach(error => { %>
        <li><%= error.msg %></li>
        <%  }) %>
    </ul>
    <% } %>

    <!-- Registration Form -->
    <form action="/account/register" method="post">
        <label for="account_firstname">First Name:</label>
        <input type="text" id="account_firstname" name="account_firstname" value="" required>

        <label for="account_lastname">Last Name:</label>
        <input type="text" id="account_lastname" name="account_lastname" value="" required>

        <label for="account_email">Email Address:</label>
        <input type="email" id="account_email" name="account_email" value="" required>

        <label for="account_password">Password:</label>
        <!-- <input type="password" id="account_password" name="account_password" required
            pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$"> -->
        <div class="password-container">
            <input type="password" id="account_password" name="account_password" required
                pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$">
            <i id="eye" class="fa-solid fa-eye-slash" onclick="togglePassword()"></i>
        </div>
        <div><i>Passwords must be minimum of 12 characters and include 1 capital letter, 1 number, 1 special
                character</i></div>
        <% if (errors && errors.account_password) { %>
        <div class="error-message"><%= errors.account_password %></div>
        <% } %>

        <button type="submit">Register</button>
    </form>